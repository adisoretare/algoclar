---
title: "Variabile și tipuri simple de date"
slug: variabile-si-tipuri-simple
grade: 5
chapter: fundamente
difficulty: baza
estimatedTime: 12
free: true
status: draft
isBridge: false
tags: ["variabile", "tipuri", "memorie", "declarare"]
visualizers: []
relatedProblems: []
quiz:
  - question: "Ce se întâmplă dacă declari o variabilă int și îi atribui valoarea 3.7?"
    options:
      - "Se păstrează 3.7"
      - "Se trunchiază la 3"
      - "Eroare de compilare"
      - "Se rotunjește la 4"
    correctIndex: 1
    explanation: "int stochează doar întregi. Valoarea 3.7 se trunchiază la 3 — partea zecimală se pierde fără avertisment."
  - question: "Care declarație este corectă în C++?"
    options:
      - "int x y z;"
      - "int x, y, z;"
      - "int x; int y int z;"
      - "int: x, y, z;"
    correctIndex: 1
    explanation: "Mai multe variabile de același tip se declară separate prin virgulă: int x, y, z;"
  - question: "De ce nu poți stoca litera 'A' într-o variabilă int?"
    options:
      - "Poți, int stochează orice"
      - "int stochează doar întregi, nu caractere"
      - "Nu poți stoca 'A' în niciun tip"
      - "Trebuie să folosești float"
    correctIndex: 1
    explanation: "int e pentru numere întregi. Caracterele se stochează în char — deși intern char e un întreg mic (codul ASCII)."
---

<LessonHook>
Gândește-te la variabile ca la niște cutii etichetate din depozit. Cutia „vârsta" conține numărul 14. Cutia „nota" conține 9.50. Cutia „litera" conține 'A'. Fiecare cutie are o etichetă (numele variabilei), un tip (ce fel de lucruri încap) și un conținut (valoarea).
</LessonHook>

## Ce este o variabilă?

O **variabilă** este un spațiu de memorie cu un nume, unde programul stochează o valoare pe care o poate citi sau modifica.

Orice variabilă are trei caracteristici:
- **Tip** — ce fel de valori acceptă (întreg, real, caracter etc.)
- **Nume** — identificatorul prin care o accesăm în cod
- **Valoare** — conținutul curent

## Tipurile simple de bază

| Tip | Ce stochează | Exemplu |
|-----|-------------|---------|
| `int` | Numere întregi | `-5`, `0`, `1000` |
| `double` | Numere reale (cu zecimale) | `3.14`, `-0.5` |
| `char` | Un singur caracter | `'A'`, `'7'`, `'!'` |
| `bool` | Valoare logică | `true`, `false` |

<ObservationBox>
`char` stochează intern un număr întreg mic (0–255) — codul ASCII al caracterului. `'A'` are codul 65. Poți face operații aritmetice cu `char`, dar e mai clar să folosești `int` pentru numere.
</ObservationBox>

## Declararea și inițializarea variabilelor

```cpp
#include <iostream>
using namespace std;

int main() {
    int varsta = 14;          // intreg, initializat la 14
    double nota = 9.50;       // real cu zecimale
    char initiala = 'A';      // un singur caracter
    bool arePermis = false;   // valoare logica

    // declarare fara initializare — valoarea e nedefinita (pericol!)
    int x;
    x = 7;                    // atribuire separata

    cout << varsta << " " << nota << " " << initiala << endl;
    return 0;
}
```

<HintBox>
Inițializează mereu variabilele la declarare. O variabilă neinițializată conține „gunoi" din memorie — valoare aleatorie care cauzează bug-uri greu de găsit.
</HintBox>

## Reguli pentru numele variabilelor

- Doar litere, cifre și `_` (underscore)
- Nu poate începe cu o cifră
- Sensibil la majuscule: `suma` ≠ `Suma` ≠ `SUMA`
- Nu poate fi un cuvânt rezervat (`int`, `for`, `if` etc.)

```
valid:   suma, nota1, medie_clasa, x
invalid: 1suma, nota-finala, int, suma finala
```

## Atribuirea valorilor

Operatorul `=` atribuie o valoare. Expresia din dreapta se evaluează **înainte** de atribuire:

```cpp
int a = 5;
int b = a + 3;    // b devine 8
a = a + 1;        // a devine 6 (nu 5+1=6 cu a vechi)
```

## Complexitate

Declararea și atribuirea unei variabile costă **O(1)** — o singură operație, indiferent de valoare.

<MistakeBox>
**Greșeala 1:** Confunzi `=` (atribuire) cu `==` (comparație). `x = 5` schimbă valoarea lui x; `x == 5` verifică dacă x e egal cu 5.

**Greșeala 2:** Declari variabila cu tipul greșit. `int medie = 9.75` trunchiază la `9`. Dacă ai nevoie de zecimale, folosește `double`.

**Greșeala 3:** Uiți să inițializezi. `int suma; suma += 10;` dă rezultat nedefinit pentru că `suma` nu are o valoare de start.
</MistakeBox>
